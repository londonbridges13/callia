<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                              <% if user_signed_in? %>
                                <strong class="font-bold"> <%= current_user.name%> </strong>
                              <% else %>
                                <strong class="font-bold"> Callia</strong>
                              <% end %>
                            </span>
                            <% if user_signed_in? %>
                            <span class="text-muted text-xs block"> <%=current_user.agency_name %> <b class="caret"></b></span>
                            <% else %>
                            <span class="text-muted text-xs block"> Dashboard <b class="caret"></b></span>
                            <% end %>
                        </span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                      <li><a href="/agency_details">Agency Details</a></li>
                      <li class="divider"></li>
                      <li><a href="/users/sign_out">Logout</a></li>
                    </ul>
                </div>
                <div class="logo-element">
                    Callia
                </div>
            </li>

            <!-- Button in Sidebar -->
            <li class="<%= is_active_controller('dashboards') %>">
                <%= link_to main_app.dashboard_index_path do %>
                <i class="fa fa-th-large"></i> <span class="nav-label" data-i18n="nav.layouts">Dashboard</span>
                  <!-- <span class="label label-primary pull-right">NEW</span> -->
                <% end %>
            </li>
            <li class="<%= is_active_controller(nil) %>">
                <a href="#"><i class="fa fa-pencil-square-o"></i> <span class="nav-label" data-i18n="nav.otherpages">Create</span><span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse <%= is_active_controller('pages', 'in') %>">
                    <li class="<%= is_active_action('new') %>"><%= link_to "Create Shift", main_app.new_shift_path %> </li>
                    <li class="<%= is_active_action('new') %>"><%= link_to "Create Client", main_app.new_client_path %> </li>
                    <li class="<%= is_active_action('new') %>"><%= link_to "Create Employee", main_app.new_caregiver_path %> </li>
                    <li class="<%= is_active_action('new') %>"><%= link_to "Create Office", main_app.new_office_path %> </li>

                </ul>
            </li>
            <li class="<%= is_active_controller('offices') %>">
                <%= link_to main_app.offices_path do %>
                <i class="fa fa-building"></i> <span class="nav-label" data-i18n="nav.layouts">Offices</span>
                  <!-- <span class="label label-primary pull-right">NEW</span> -->
                <% end %>
            </li>
            <li class="<%= is_active_controller('clients') %>">
                <%= link_to main_app.clients_path do %>
                <i class="fa fa-group (alias)"></i> <span class="nav-label" data-i18n="nav.layouts">Clients</span>
                  <!-- <span class="label label-primary pull-right">NEW</span> -->
                <% end %>
            </li>
            <li class="<%= is_active_controller('caregivers') %>">
                <%= link_to main_app.caregivers_path do %>
                <i class="fa fa-user-md"></i> <span class="nav-label" data-i18n="nav.layouts">Employees</span>
                  <!-- <span class="label label-primary pull-right">NEW</span> -->
                <% end %>
            </li>
            <li class="<%= is_active_controller('reports') %>">
                <%= link_to main_app.reports_path do %>
                <i class="fa fa-file-text-o"></i> <span class="nav-label" data-i18n="nav.layouts">Reports</span>
                  <!-- <span class="label label-primary pull-right">NEW</span> -->
                <% end %>
            </li>
            <li class="<%= is_active_controller('settings') %>">
                <%= link_to main_app.settings_path do %>
                <i class="fa fa-gear"></i> <span class="nav-label" data-i18n="nav.layouts">Settings</span>
                  <!-- <span class="label label-primary pull-right">NEW</span> -->
                <% end %>
            </li>
        </ul>

    </div>
</nav>
