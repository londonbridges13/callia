<%= form_for(@recurring_shift) do |f| %>
  <% if @recurring_shift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recurring_shift.errors.count, "error") %> prohibited this recurring_shift from being saved:</h2>

      <ul>
      <% @recurring_shift.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :frequency %><br>
    <%= f.text_field :frequency %>
  </div>
  <div class="field">
    <%= f.label :time_span %><br>
    <%= f.text_field :time_span %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
